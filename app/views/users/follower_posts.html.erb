<h1>Follower Posts for user <%= @user.id %></h1>

<% unless @posts.empty? %>
  <h2>Posts</h2>
  <table>
    <tr>
      <th>Text</th>
      <th>Time</th>
    </tr>
    <%= render @posts %>
  </table>
<% end %>

<%= link_to 'Back', user_path(@user) %>
